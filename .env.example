# Local server port. No secrets are required for the default local-only flow.
PORT=3000

# Default local model endpoint expected by the server when no saved settings exist:
LOCAL_MODEL_EP=http://127.0.0.1:1234/v1
